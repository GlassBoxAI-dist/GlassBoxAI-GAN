[build-system]
requires      = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name            = "facaded_gan"
version         = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
manifest-path = "python_bindings/Cargo.toml"
